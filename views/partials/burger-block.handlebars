<li>
    {{ burger_name }}

    <button class="devour-burger" data-id="{{id}}" data-newdevour="{{devour}}">
        {{#if devour}}DEVOUR TIME!{{ else}}WHIP UP ANOTHER BURGER!{{/if}}
    </button>

</li>